import {TextReveal,Reveal} from "@/components/motion/reveal";
import {MotionCTA} from "@/components/motion/cta";
import {HeroVisual} from "@/components/motion/hero-visual";
import Link from "next/link";
import {ArrowRight} from "lucide-react";
import type {Vehicle} from "@/types";
import {VehicleImage} from "@/components/cars/vehicle-image";
export function Hero({vehicle}:{vehicle?:Vehicle}){return <section className="home-hero"><div className="container"><div className="hero-heading"><div><p className="eyebrow">For the road ahead · Nigeria</p><TextReveal as="h1" hero lines={["Find the car.","That fits your life."]}/></div><Reveal delay={.2} className="hero-intro"><p>Discover, evaluate and compare cars with your budget and priorities in the driving seat.</p><div className="mt-6 inline-block"><MotionCTA href="/car-adviser" magnetic>Find my car</MotionCTA></div><Link href="/cars" className="ml-5 inline-flex items-center gap-2 text-sm font-bold">Browse cars <ArrowRight size={16}/></Link></Reveal></div><HeroVisual image={vehicle?<VehicleImage vehicle={vehicle} preload sizes="(max-width: 1200px) 100vw, 1180px"/>:null}><div className="hero-caption"><span className="technical">01 / A clearer way to choose</span><p>More than a good-looking car.<br/>A good decision.</p><span className="text-sm text-white/75">Not sure what to buy? Start with a few guided questions.</span></div>{vehicle&&<Link href={`/cars/${vehicle.slug}`} className="hero-spec"><span className="technical">Explore this listing ↗</span><strong>{vehicle.make} {vehicle.model}</strong><span>{vehicle.year} · {vehicle.transmission.toLowerCase()} · {vehicle.bodyType.toLowerCase()}</span></Link>}</HeroVisual><div className="hero-bottom"><span>YOUR PRIORITIES. YOUR SHORTLIST.</span><span>Discover <i>→</i> Decide <i>→</i> Compare <i>→</i> Save</span></div></div></section>}
