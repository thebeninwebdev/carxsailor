"use client";
import {useState} from "react";
import type {AdviserResponse} from "@/lib/ai/types";
import {Results} from "./results";
export function Adviser(){const[text,setText]=useState("");const[data,setData]=useState<AdviserResponse|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();if(busy)return;setBusy(true);setError("");try{const r=await fetch("/api/adviser/recommend",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({message:text})});const result=await r.json();if(!r.ok)throw new Error(result.error);setData(result);if(!result.clarification)try{sessionStorage.setItem("carxsailor-results",JSON.stringify(result))}catch{}}catch(e){setError(e instanceof Error?e.message:"Please try again.")}finally{setBusy(false)}}return <div><form className="question-panel" onSubmit={submit}><label htmlFor="car-request" className="label">Anything else you want in your car?</label><p className="mb-4 text-sm leading-6 text-[#69756f]">Optional: describe anything you did not cover in the guide. We will only use details that map to stored listing information.</p><textarea id="car-request" className="input min-h-32" minLength={3} maxLength={3000} value={text} onChange={e=>setText(e.target.value)} placeholder="e.g. I want a comfortable automatic SUV for family use and Nigerian roads"/>{data?.clarification&&<p role="status" className="my-4 rounded-xl bg-amber-50 p-4">{data.clarification} Add your answer above and try again, or use guided mode.</p>}{error&&<p role="alert" className="mt-4 text-red-700">{error}</p>}<button disabled={busy} className="btn btn-dark mt-5">{busy?"Comparing cars...":"Find my cars"}</button></form>{data&&!data.clarification&&<Results data={data}/>}</div>}
