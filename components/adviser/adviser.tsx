"use client";
import {useState} from "react";
import type {AdviserResponse} from "@/lib/ai/types";
import {Results} from "./results";
export function Adviser(){const[text,setText]=useState("");const[data,setData]=useState<AdviserResponse|null>(null);const[busy,setBusy]=useState(false);const[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError("");try{const r=await fetch("/api/adviser/recommend",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({message:text})});const result=await r.json();if(!r.ok)throw new Error(result.error);setData(result);if(!result.clarification)try{sessionStorage.setItem("carxsailor-results",JSON.stringify(result))}catch{}}catch(e){setError(e instanceof Error?e.message:"Please try again.")}finally{setBusy(false)}}return <div><form className="question-panel" onSubmit={submit}><label htmlFor="car-request" className="label">Describe the car you need</label><p className="mb-4 text-sm leading-6 text-[#69756f]">Include your maximum budget in millions of naira, transmission and priorities. Review the interpreted requirements in your results.</p><textarea id="car-request" className="input min-h-32" minLength={3} maxLength={3000} required value={text} onChange={e=>setText(e.target.value)} placeholder="My budget is 8 million naira. I want an automatic with low maintenance costs and good fuel economy."/>{data?.clarification&&<p role="status" className="my-4 rounded-xl bg-amber-50 p-4">{data.clarification} Add your answer above and try again, or use guided mode.</p>}{error&&<p role="alert" className="mt-4 text-red-700">{error}</p>}<button disabled={busy} className="btn btn-dark mt-5">{busy?"Comparing cars...":"Find my cars"}</button></form>{data&&!data.clarification&&<Results data={data}/>}</div>}
