"use client";
import Link from "next/link";
import {useState} from "react";
import {useRouter} from "next/navigation";
import {signIn,signUp} from "@/lib/auth-client";
import {safeNext} from "@/lib/safe-next";
export function AuthForm({mode,redirectTo="/dashboard"}:{mode:"login"|"register";redirectTo?:string}){const router=useRouter();const[error,setError]=useState("");const[loading,setLoading]=useState(false);const destination=safeNext(redirectTo);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const f=new FormData(e.currentTarget);try{const email=String(f.get("email")),password=String(f.get("password"));const result=mode==="login"?await signIn.email({email,password}):await signUp.email({email,password,name:String(f.get("name"))});if(result.error)throw new Error(result.error.message||"Authentication failed");try{const id=sessionStorage.getItem("carxsailor-pending-save");if(id){const saved=await fetch("/api/favorites",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({vehicleId:id})});if(saved.ok)sessionStorage.removeItem("carxsailor-pending-save")}}catch{/* The returned listing lets the user retry saving. */}router.replace(destination);router.refresh()}catch(e){setError(e instanceof Error?e.message:"Could not sign in. Please try again.")}finally{setLoading(false)}}const other=(mode==="login"?"/register":"/login")+"?next="+encodeURIComponent(destination);return <form onSubmit={submit} className="grid gap-4">{mode==="register"&&<label><span className="label">Full name</span><input className="input" name="name" minLength={2} required autoComplete="name"/></label>}<label><span className="label">Email address</span><input className="input" name="email" type="email" required autoComplete="email"/></label><label><span className="label">Password</span><input className="input" name="password" type="password" minLength={8} required autoComplete={mode==="login"?"current-password":"new-password"}/></label>{error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-800">{error}</p>}<button disabled={loading} className="btn btn-dark">{loading?"Please wait...":mode==="login"?"Sign in":"Create account"}</button><p className="text-center text-sm text-[#68756f]">{mode==="login"?"New to CarXSailor? ":"Already have an account? "}<Link className="font-bold text-[#245c47]" href={other}>{mode==="login"?"Create account":"Sign in"}</Link></p></form>}
