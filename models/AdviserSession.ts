import mongoose,{Schema} from "mongoose";const schema=new Schema({userId:{type:String,index:true},anonymousId:{type:String,index:true},messages:[{role:{type:String,enum:["user","assistant"]},content:String}],preference:Schema.Types.Mixed,recommendations:[{vehicleId:String,score:Number}],status:{type:String,enum:["ACTIVE","COMPLETED"],default:"ACTIVE"}},{timestamps:true});export const AdviserSessionModel=mongoose.models.AdviserSession||mongoose.model("AdviserSession",schema);
