import mongoose,{Schema} from "mongoose";const schema=new Schema({vehicleId:{type:Schema.Types.ObjectId,required:true,index:true,unique:true},inspectorId:String,status:{type:String,enum:["PENDING","VERIFIED","REJECTED"],default:"PENDING"},scores:{type:Map,of:{type:Number,min:0,max:100}},notes:String,inspectedAt:Date},{timestamps:true});export const VehicleInspectionModel=mongoose.models.VehicleInspection||mongoose.model("VehicleInspection",schema);
