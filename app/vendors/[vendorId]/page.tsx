import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{vendorId:string}>}){const{vendorId}=await params;if(!vendorId)notFound();return <div className="container py-16"><div className="card p-8"><p className="eyebrow">Approved vendor</p><h1 className="display mt-3 text-4xl">Coastline Motors</h1><p className="mt-4 max-w-xl leading-7 text-[#68756f]">A public vendor profile shows display information and active inventory only. Private phone and verification records are never exposed here.</p></div></div>}
