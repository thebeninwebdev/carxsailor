import type {MetadataRoute} from "next";import {listActiveVehicles} from "@/lib/vehicles";export const dynamic="force-dynamic";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const base=process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000",vehicles=await listActiveVehicles({},1000);return["","/cars","/car-adviser","/compare","/about","/how-it-works","/contact","/become-a-vendor",...vehicles.map(v=>`/cars/${v.slug}`)].map(url=>({url:base+url,lastModified:new Date(),changeFrequency:url.startsWith("/cars")?"daily":"monthly"}))}
